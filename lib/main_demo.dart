import 'package:flutter/material.dart';
import 'features/demo/demo_app.dart';

// Deliberately independent of bootstrap: no Supabase, Firebase or remote fonts.
void main() => runApp(const DemoApp());
